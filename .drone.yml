pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/gitea
        tags: [1.6.0-dev, latest]
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
